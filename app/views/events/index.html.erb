<% provide(:title, 'All Events') %>
<h1>All Events</h1>

<%= will_paginate %>

<ul class="events">
  <% @events.each do |e| %>
    <li>
        <%= link_to e.name, "/events/#{e.id}" %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
